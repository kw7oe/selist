<% if current_user.is_teacher? %>
  <div class = "settings">
    <a class = "icon settings-link" data-behavior="toggle_settings"></a>
    <ul class = "dropdown-menu">
      <li><%= link_to "Edit Subject", edit_subject_path(subject) %></li>
      <li><%= link_to "Delete Subject", subject_path(subject), remote: true, method: :delete, data: { confirm: "Are you sure?"} %></li>
    </ul>
  </div>
<% end %>