<nav class="nav">
  <div class="nav-header">
    <%= platform_name %>
  </div>
  <div class="dropdown-toggle" >
    <div class="dash"></div>
    <div class="dash"></div>
    <div class="dash"></div>
  </div>
  <ul class="dropdown">
    <li>About</li>
    <li>Features</li>
    <% if current_user %>
      Logged in as <%= current_user.email %>
      <%= link_to "Signed Out", session_path("current"), method: "delete" %>
    <% else %>
      <li> <%= link_to "Sign Up", new_user_path %> </li>
    <% end %>
  </ul>
</nav>
